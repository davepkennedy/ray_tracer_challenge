#include "pch.h"
#include "tuple.h"

/*
rt::tuple rt::point(float x, float y, float z)
{
	return rt::tuple(x, y, z, 1);
}

rt::tuple rt::vector(float x, float y, float z)
{
	return rt::tuple(x, y, z, 0);
}

float rt::dot(const rt::tuple& a, const rt::tuple& b)
{
	return a.x()* b.x() +
		a.y() * b.y() +
		a.z() * b.z() +
		a.w() * b.w();
}

rt::tuple rt::cross(const rt::tuple& a, const rt::tuple& b)
{
	return rt::vector(
		a.y() * b.z() - a.z() * b.y(),
		a.z() * b.x() - a.x() * b.z(),
		a.x() * b.y() - a.y() * b.x()
	);
}
*/